<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobileapp.scheduler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Scheduler">
        <activity android:name=".ui.timer.TempTimer"></activity>
        <activity android:name=".ui.membership.SigninActivity" />
        <activity android:name=".ui.membership.SignupActivity" />
        <activity android:name=".ui.calender.AddCalendarActivty" />
        <activity android:name=".ui.diary.AddDiaryActivty" />
        <activity android:name=".ui.memo.AddMemoActivty" />
        <service android:name=".ui.timer.AlarmService"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>